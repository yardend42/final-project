export class followers {
  public user_id: number;
  public vacation_id: number;

  constructor(user_id: number, vacation_id: number) {
    this.user_id = user_id;
    this.vacation_id = vacation_id;
  }
}
